name : Build and Deploy Code
on: [push, pull_request]

jobs:
    job1:
        runs-on : ubuntu-latest
        steps:
            - name: pulling git repo
              uses: actions/checkout@v2
            - name: say hi to nstn
              run : echo "hello nstn this is my yml code and i want to workflow to github but it failure?"  
